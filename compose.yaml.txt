services:
  backend:
    image: python:3.11
    build:
      context: ./backend
    command: uvicorn main:app --host 0.0.0.0 --port 5000
    ports:
      - "5000:5000"

  java-service:
    image: openjdk:17
    build:
      context: ./java-service
    command: java -jar chatbot.jar
    ports:
      - "8080:8080"

  frontend:
    image: nginx:latest
    build:
      context: ./frontend
    ports:
      - "80:80"
